spring:

  datasource:
    url: jdbc:mariadb://mariadb_cdc:3306/dachserboard
    driver-class-name: org.mariadb.jdbc.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  sql:
    init:
      mode: never

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: false

  h2:
    console:
      enabled: false

app:

  security:
    enable-login: true

  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: 28800000
